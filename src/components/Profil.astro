<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>
  <body>
    <div class="container">
      <div class="card">
        <div class="content">
          <div class="img"><img src="../public/Profil.jpeg"></div>
          <div class="cardContent">
            <h3>Dirk Wening<br><span>Umschüler Fachinformatiker<br><span>Systemintegration</span></h3>
          </div>
        </div>
        <ul class="sci">
          <a href="https://www.linkedin.com/in/dirk-wening-38b7a22b2/" target="_blank" >
            <li><i class="fa fa-linkedin" style="color: #007bff;"></i></li>
          </a>
          <a href="https://github.com/SpeedD3" target="_blank">
          <li><i class="fa fa-github" style="color: #6cc644;"></i></li>
          </a>  
        </ul>
 
      </div>
     
    </div>
  </body>
  <style>
 
$colors: (
  one:     #990033,
  two:     #161623,
 
  negro:  #020202,
  blanco: #ffffff,
  sombra: #000000
);
 
@function color($color-name) {
    @return map-get($colors, $color-name);
}
 
@mixin displayFlex($justify,$align,$direction) {
    display: flex;
    justify-content: $justify;
    align-items: $align;
    flex-direction: $direction;
}
 
@keyframes colorful{
  0% {
    filter: hue-rotate(0deg);
  }
  100% {
    filter: hue-rotate(360deg);
  }
}
 
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  text-decoration: none;
  list-style: none;
  outline: none;
  appearance: none;
  border-style: none;
  color: color(blanco);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
 
 
h1,h2,h3,span,p {
  font-family: "Montserrat", -apple-system, BlinkMacSystemFont, "Segoe UI",
    "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans",
    "Helvetica Neue", sans-serif;
}
 
html,
body {
  width: 100%;
  height: 100%;
  background: color #943636;
 
  /*scrollbar-width: none;
  overflow: hidden;
 
  &::-webkit-scrollbar {
    width: 0;
  }*/
}
 
body {
  position: relative;
  overflow: hidden;
  @include displayFlex(center, center, column);
  padding: 2em;
  flex-direction: column;
  justify-items: center;
  align-items: center;
  gap: 2rem;
 
  &::-webkit-scrollbar {
    background: color(negro);
    width: 10px;
  }
  &::-webkit-scrollbar-track {
    background: color #b10f0f;
  }
  &::-webkit-scrollbar-thumb {
    background: color #b10f0f;
    border-radius: 1px;
  }
 
  .container {
    position: relative;
    left: 40%;
    top: 9%;
    z-index: 1;
    @include displayFlex(center, center, row);
    flex-wrap: wrap;
    padding: 1em;
   
    .card {
      position: relative; /* Position Karte */
      top: 15vh;
      left: 100%;
      width: 300px;
      height: 400px;
      margin: 1em;
      background: rgba(44, 53, 49, 1);
      box-shadow: 0 4px 30px rgb(192, 9, 9);
      backdrop-filter: blur(4px);
      border-radius: 30px;
      @include displayFlex(center, center, column);
      backdrop-filter: blur(40px);
      z-index: 9;
      background-clip: padding-box;
      box-shadow: 0 0 5px #FFCB9A, 0 0 15px #FFCB9A, 0 0 30px #FFCB9A,
      0 0 60px #FFCB9A;
      
     
      .content {
        position: relative;
        @include displayFlex(center, center, column);
        opacity: 1.5;
        transition: 0.5s;
       
        .img {
          position: relative;
          top: 5vh;
          left: 8vh;
          width: 150px; height: 150px;
          border-radius: 20%;
          overflow: hidden;
          border: 2px solid #FFCB9A;
          

          
 
          img {
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            object-fit: cover;
          }
        }
       
        .cardContent {    /*--- Schriftfarbe---*/
          h3 {
            color: #D1E8E2;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 00;
            font-size: 18px;
            text-align: center;
            
            margin: 30% 0 10px;
            line-height: 1.1em;
           
           
            span {
              font-size: 12px;
              font-weight: 300;
              text-transform: initial;
            }
          }
        }
      }
     
      .sci {
        display:flex;
        flex direction:row;
        justify-content: center;
        align-items: center;
        bottom: 500px;
       
       
        li {
          margin: 0 10px;
          transform: translateX(40px);
          opacity: 0;
          transition: 0.5s;
          transition-delay: calc(0.1s * var(--i));
          scale: 10px;
 
          a {
            font-size: 29px;
          }
        }
      }
     
      &:hover .content {
        opacity: 1;
        transform: translateY(-20px);
      }
     
      &:hover .sci li {
        transform: translateY(0px);
        opacity: 1;
      }
    }
  }
}
 
 
  </style>